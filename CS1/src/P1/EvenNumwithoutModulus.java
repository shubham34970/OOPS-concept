package P1;

import java.util.Scanner;

public class EvenNumwithoutModulus {
    Scanner in = new Scanner(System.in);
    int num = in.nextInt();

    // using bitwise operator(&&)
    // using divide by 2 multiply by 2
    // by continuous subtracting 2
    // by multiplying with 5(using string)
    // By checking last digit(using string)
}
